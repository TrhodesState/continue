name: Local Config
version: 0.0.1
schema: v1

models:
  - name: GPT-5.3 Codex (Azure)
    provider: azure
    model: gpt-5.3-codex
    apiBase: https://tomr-mlh7ywiq-eastus2.cognitiveservices.azure.com
    apiKey: ${{ secrets.AZURE_OPENAI_API_KEY }}
    env:
      deployment: gpt-5.3-codex
      apiType: azure-openai
      apiVersion: "2025-04-01-preview"
    roles:
      - chat
